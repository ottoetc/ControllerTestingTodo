<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>@ViewBag.Title</title>
    <link rel="stylesheet" type="text/css" href="~/css/StyleSheet.css" />

</head>
<body>
    <div>
        @RenderBody()
    </div>
    <p>
        @Html.ActionLink("LIST", "Index", "Items") |
        @Html.ActionLink("HOME", "Index", "Home")
    </p>
</body>
</html>
